import { createFileRoute, Link } from "@tanstack/react-router";
import { Filter, MapPin, SlidersHorizontal } from "lucide-react";
import { AppHeader, ArtistCard, MobileTabs, Stars, artists } from "@/components/glowtique";
import { Button } from "@/components/ui/button";

export const Route = createFileRoute("/browse")({ head: () => ({ meta: [{ title: "Discover artists — Glowtique" }, { name: "description", content: "Browse verified makeup artists and beauticians by city, service and rating." }, { property: "og:title", content: "Discover artists — Glowtique" }, { property: "og:description", content: "Browse verified makeup artists and beauticians by city, service and rating." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary_large_image" }] }), component: Browse });
function Browse() { return <div className="min-h-screen bg-wine text-ivory"><AppHeader/><main className="mx-auto max-w-7xl px-5 pb-28 pt-10 sm:px-8 lg:px-12"><div className="flex items-end justify-between gap-4"><div><p className="font-mono text-[10px] tracking-[0.2em] text-blush">DISCOVER YOUR ARTIST</p><h1 className="mt-2 font-serif text-4xl sm:text-5xl">Browse the edit</h1><p className="mt-2 text-sm text-ivory/55">124 artists ready for your next occasion</p></div><Button variant="outline" className="rounded-xl border-white/15 text-ivory hover:bg-white/10"><SlidersHorizontal/> Filters</Button></div><div className="mt-8 flex flex-wrap gap-2"><span className="rounded-full bg-gold px-4 py-2 text-xs font-medium text-wine">All artists</span>{["Bridal", "Party glam", "Hair styling", "Mehendi", "HD makeup"].map((item) => <button key={item} type="button" className="rounded-full bg-white/5 px-4 py-2 text-xs text-ivory/70 ring-1 ring-white/10 hover:bg-white/10">{item}</button>)}</div><div className="mt-8 grid gap-8 lg:grid-cols-[220px_1fr]"><aside className="hidden space-y-6 rounded-2xl bg-wine-700 p-5 ring-1 ring-white/10 lg:block"><div><p className="font-mono text-[10px] tracking-widest text-blush">LOCATION</p><div className="mt-3 flex items-center gap-2 rounded-xl bg-cream px-3 py-3 text-sm text-wine"><MapPin className="size-4"/> Mumbai <span className="ml-auto">⌄</span></div></div><div><p className="font-mono text-[10px] tracking-widest text-blush">PRICE RANGE</p><div className="mt-4 h-1 rounded-full bg-white/15"><div className="h-1 w-2/3 rounded-full bg-gold"/></div><div className="mt-3 flex justify-between text-xs text-ivory/60"><span>₹2,000</span><span>₹20,000+</span></div></div><div><p className="font-mono text-[10px] tracking-widest text-blush">RATING</p>{["4.5+", "4.0+", "All ratings"].map((rating) => <div key={rating} className="mt-3 flex items-center gap-2 text-sm text-ivory/70"><span className="grid size-4 place-items-center rounded border border-white/20">{rating === "4.5+" ? "✓" : ""}</span><Stars rating={rating === "4.5+" ? 4.5 : 4}/></div>)}</div><Button className="w-full rounded-xl bg-gold text-wine hover:bg-gold/90"><Filter/> Apply filters</Button></aside><section><div className="mb-4 flex items-center justify-between text-xs text-ivory/50"><span>Showing curated results</span><button type="button" className="text-blush">Sort: Recommended⌄</button></div><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{artists.map((artist) => <ArtistCard key={artist.id} artist={artist}/>)}</div></section></div></main><MobileTabs active="search"/></div>; }